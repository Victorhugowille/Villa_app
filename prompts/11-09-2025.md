### 📄 Arquivo: `prompts/11-09-2025.md`
**Foco:** A Regra de Ouro e Banco de Dados.

```markdown
# Prompt: Padrão de Código e Tabelas

**Data:** 11/09/2025
**Contexto:** Definição de como a IA deve responder e criação da primeira tabela crítica.

## 🗣️ Prompt do Usuário
"Eu gosto de códigos simples e diretos, seguindo minhas instruções.
Quando eu mandar um codigo ou um erro, quero que você me mande o codigo inteiro corrigido e fale em qual pasta e, seja preciso.

Agora, crie o código SQL para a tabela de 'mesas' no Supabase. Ela precisa ter o número e saber se está ocupada."

## 🤖 Resposta (SQL e Regra Aceita)

Entendido. Sempre enviarei o arquivo completo.

### Script SQL Gerado:
```sql
-- Criação da tabela de Mesas
create table public.mesas (
  id bigint generated by default as identity primary key,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  numero integer not null,
  status text default 'livre'::text, -- Valores: 'livre', 'ocupada', 'pagando'
  company_id uuid references auth.users(id) -- Vínculo com a empresa (Tenant)
);

-- Habilitar segurança básica
alter table public.mesas enable row level security;
